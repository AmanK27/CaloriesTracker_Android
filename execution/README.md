# Execution

This directory contains deterministic Python scripts (tools) that carry out concrete tasks (e.g., API calls, database operations, data parsing).

## Guidelines
- Write scripts in Python.
- Keep scripts focused, modular, and single-purpose.
- Load environment variables from `.env` dynamically.
- Implement thorough error handling and descriptive logging/exit codes.
- Do not hardcode secrets or keys.
